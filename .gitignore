# Dependencies
app/node_modules/

# Build output
app/dist/

# Vite / TypeScript cache
app/.vite/
app/node_modules/.tmp/

# Environment & secrets
.env
.env.*
!.env.example

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
.idea/

# PWA dev files (generated at build time)
app/dev-dist/

# Claude Code / Agent SDK — installed skills (like node_modules; use skills-lock.json instead)
.claude/
.agents/

# Data directory
data/

# Notas de investigação de performance — deliberadamente fora do controle de versão (ver o
# próprio cabeçalho do arquivo). Acabou commitado sem querer no M-72 (8a9d8ea); untracked aqui
# pra não repetir.
plan/PERFORMANCE.md

# Python bytecode cache (scripts/sync_gimbo.py)
__pycache__/
*.pyc

# Test & coverage artifacts
app/coverage/
app/playwright-report/
app/test-results/
